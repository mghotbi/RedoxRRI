% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_RRI_ternary.R
\name{plot_RRI_ternary}
\alias{plot_RRI_ternary}
\title{Holobiont Redox Resilience Triangle}
\usage{
plot_RRI_ternary(
  ternary_df,
  point_size = 5,
  point_alpha = 0.9,
  palette = "plasma",
  show_subtitle = TRUE
)
}
\arguments{
\item{ternary_df}{A data frame produced by \code{\link{RRI_pipeline}}.}

\item{point_size}{Numeric; point size (default \code{5}).}

\item{point_alpha}{Numeric; point alpha (default \code{0.90}).}

\item{palette}{Character; viridis palette option (default \code{"plasma"}).}

\item{show_subtitle}{Logical; include system mean RRI subtitle (default \code{TRUE}).}
}
\value{
A \code{ggtern} object.
}
\description{
Creates an ISME-style ternary plot of holobiont redox resilience based on the
output of \code{\link{RRI_pipeline}}. Points are positioned by the compositional
domain contributions (\code{Physio}, \code{Soil}, \code{Micro}) and colored by the
per-sample \code{RRI}. The system-level mean RRI (\code{attr(x, "RRI_index")}) is
shown in the subtitle.
}
\details{
Plot the Holobiont Redox Resilience Ternary Diagram

This function expects a data frame with columns:
\itemize{
\item \code{Physio}, \code{Soil}, \code{Micro} (compositional; should sum to 1 per row),
\item \code{RRI} (0–1).
}

Plotting packages are treated as \strong{optional} dependencies (listed in \code{Suggests}).
If not installed, the function stops with a clear message.
}
\section{Suggested packages}{

\itemize{
\item \pkg{ggtern}
\item \pkg{ggplot2}
\item \pkg{viridis}
}
}

\examples{
# Requires ggtern + ggplot2 + viridis installed
# out <- RRI_pipeline(...)
# p <- plot_RRI_ternary(out)
# print(p)
}
